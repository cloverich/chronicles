/**
 * Editor mode options toggling between regular ("editor") and various debug modes.
 */
export enum EditorMode {
  // EditorV2 = "editor-v2",
  Editor = "editor-v2",
  SlateDom = "slate-dom",
  Mdast = "mdast",
  Markdown = "markdown",
}
