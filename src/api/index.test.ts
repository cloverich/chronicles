import { describe, it } from "mocha";
import { assert } from "chai";

describe("api", function () {
  describe("journals", function () {
    it("requires name on create"); // return 400, title is "Journal name is required"
  });
});
