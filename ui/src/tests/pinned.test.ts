import { suite, test } from "mocha";

// Alternative: Focused heading, Focus
suite("Pinned headings", function () {
  test("changing journal (or search) re-sets the pinned heading");
  test("pinning a heading ");
  test("unfocusing a heading clears the search");
  test("unfocusing a heading removes ");
});
